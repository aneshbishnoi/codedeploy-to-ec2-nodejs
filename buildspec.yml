version: 0.2

phases:
  install: 
    runtime-versions:
      nodejs: 10
  build:
    commands:
      - echo "Package & Dependicies are getting installed"
      - npm -f install
      - echo "Packages have been installed"
artifacts:
  type: zip
  paths:
    - '*'
